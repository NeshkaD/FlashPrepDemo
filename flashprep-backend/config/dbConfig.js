// Devs: change conf variable below to match the database running in your dev environment
let conf = {
    host: "localhost",
    user: "root",
    password: "password",
    database: "flashprep"
};

module.exports = conf;